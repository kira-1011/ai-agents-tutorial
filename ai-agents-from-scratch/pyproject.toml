[project]
name = "ai-agents-from-scratch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "keyboard>=0.13.5",
    "requests>=2.32.5",
]

[dependency-groups]
dev = [
    "ruff>=0.15.4",
]
